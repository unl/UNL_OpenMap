<?php

$image_dir = dirname(dirname(dirname(dirname(dirname(__FILE__))))).'/BuildingImages/';
if (file_exists($image_dir.$context->code.'_01_'.$context->size.'.jpg')) {
    header('Content-type: image/jpeg');
    echo file_get_contents($image_dir.$context->code.'_01_'.$context->size.'.jpg');
    exit();
}

echo $savvy->render($context, 'UNL/TourMap/BuildingImage/Missing.tpl.php');
